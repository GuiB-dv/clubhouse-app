extends layout

block content
  h1= title
  
  hr
  if msg.length
    ul
      each message in msg
        li
          dt
            | #{message.title}
          dd  #{message.message} 
            a(href=message.url) details

  else
    p There are no messages.